@page "/basic"

<div class="container mx-auto px-4 py-12">
      <h1 class="text-3xl font-bold mb-6">Basic Image Example</h1>
      <p class="mb-8">
        This example shows the basic usage of the Image component with explicit width and height. The image is
        automatically optimized and will lazy load by default.
      </p>

      <div class="border p-4 rounded-lg">
        <Image Src="/images/photo.jpeg"
          Alt="Example image"
          Width="600"
          Height="400"
               Caption="basic usage of the Image component" />

        <div class="mt-4 text-sm text-gray-600">
          <p class="font-medium">Key features:</p>
          <ul class="list-disc pl-5 mt-2">
            <li>Automatic image optimization</li>
            <li>WebP format conversion by default</li>
            <li>Quality is 75 by default</li>
            <li>Prevents layout shift with explicit dimensions</li>
            <li>Lazy loaded by default</li>
          </ul>
        </div>
      </div>

      <div class="mt-8">
        <h2 class="text-xl font-semibold mb-4">Code Example</h2>
        <pre class="bg-gray-100 p-4 rounded-md overflow-x-auto">
@("<")Image
    Src="/example-image.jpg"
    Alt="Example image"
    Width="600"
    Height="400"
    Caption="basic usage of the Image component"
@("/>")
        </pre>
      </div>
    </div>